module.exports = function (application) {
    // Rota de Criacao de Rotinas do Usuario
    application.post('/user/:idUser/routines', function (req, res) {

    });

    // Rota para Retorno da Lista de Rotinas do Usuario
    application.get('/user/:idUser/routines', function (req, res) {

    });

    // Rota para Retorno de uma Rotina pelo ID
    application.get('/user/:idUser/routines/:idRoutine', function (req, res) {

    });

    // Rota para Atualizar Rotina pelo ID
    application.put('/user/:idUser/videos/:idRoutine', function (req, res) {

    });

    // Rota para Procura de Video por Chave
    // Rota para Deletar Video por ID

}